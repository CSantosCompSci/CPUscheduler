
public class Processor {

	int process, arrivalTime, burstTime,priority;  
	public Processor(int readProcess, int readArrivalTime,  int readBurstTime, int readPriority)
	{
		process = readProcess;
		arrivalTime = readArrivalTime;
		burstTime = readBurstTime;
		priority = readPriority;
	}
	
	/*method that returns the value of the priority of the job
	 * @param
	 *
	 */
	public int getPriority()
	{
		
	}
		
}
